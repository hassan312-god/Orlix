[package]
name = "orlix-studio"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.0.0-beta.10", features = ["shell-open"] }

[build-dependencies]
tauri-build = { version = "2.0.0-beta.10" }
